# Campus Opinion Board 작업 지침

## 목표

이 프로젝트는 비전공 대학생에게 브라우저, HTTP, 백엔드, 데이터베이스와 Cloudflare 배포의 연결을 설명하기 위한 최소 기능 제품이다.

## 기술 제약

- Next.js App Router 호환 API와 vinext를 사용한다.
- 배포 대상은 Cloudflare Workers다.
- 데이터 저장은 Cloudflare D1의 `DB` 바인딩을 사용한다.
- 데이터베이스 접근은 준비된 SQL과 `bind()`로 처리한다.
- 의견 등록과 목록 조회는 `/api/opinions`, 수정과 삭제는
  `/api/opinions/[id]` Route Handler를 거친다.
- 요청·응답·데이터베이스 행에 명시적인 TypeScript 타입을 사용한다.

## 범위

- 핵심 기능은 의견 등록·목록 조회·수정·삭제와 새로고침이다.
- 요청 없이 인증, 댓글, 좋아요, 파일 업로드, 검색, 실시간 기능을 추가하지 않는다.
- 수정·삭제는 게시글을 등록할 때 지정한 비밀번호를 검증한 뒤에만 허용한다.

## 보안

- 닉네임과 의견은 클라이언트와 서버에서 모두 검증한다.
- SQL 문자열에 사용자 입력을 직접 연결하지 않는다.
- 학번, 이메일, IP 주소 등 개인정보를 저장하지 않는다.
- 사용자 입력 전체나 비밀값을 로그에 남기지 않는다.
- 비밀값을 `NEXT_PUBLIC_` 변수나 저장소에 넣지 않는다.
- 게시글 비밀번호 원문을 저장·응답·로그에 남기지 않는다.
- API 내부 오류를 사용자 응답에 그대로 노출하지 않는다.

## 변경 및 검증

- 패키지나 데이터베이스 스키마 변경은 사전에 이유와 영향을 설명한다.
- 스키마 변경은 새 D1 마이그레이션 파일로 추가한다.
- 변경 후 `pnpm typecheck`와 `pnpm build`를 실행한다.
- 배포 전 원격 D1 마이그레이션 적용 여부와 `wrangler.jsonc`의 데이터베이스 ID를 확인한다.
